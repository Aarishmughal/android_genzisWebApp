<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.TheLegacy" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorOnPrimary">@color/branding</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.TheLegacy" parent="Base.Theme.TheLegacy" />
</resources>